# Parked functions — known unmatchable with current toolchain.
#
# Functions listed here are skipped by `tools/find_leaves.py --exclude-parked`.
# Each entry should have a brief reason and (optionally) a pointer to a
# tough_nuts/<func>/notes.md write-up.
#
# Format:
#   func_NAME    # one-line reason (and pointer to tough_nuts notes)
#
# Blank lines and `# ...` comments are ignored. Entries are matched as
# exact function names (whitespace-trimmed, comment-stripped).
#
# To add a new parked function:
#   1. Write up the failure mode in `tough_nuts/<func>/notes.md`.
#   2. Append the function name + reason here.
#   3. (Optional) Commit `Park func_X — <reason>`.
#
# Re-evaluate periodically as the compiler identification firms up
# (see `decomp/NOTES.md` "Compiler fingerprint" section).

# ---- la-macro 64-bit expansion (ee-gcc 2.96 emits lui+daddiu+daddu;
# original uses lui+addiu+addu with different scheduling).
# See tough_nuts/func_00105278/notes.md.
func_00105278  # la-macro-64-bit (parked, see tough_nuts/func_00105278/notes.md)
func_00105290  # la-macro-64-bit (sibling of 00105278)

# ---- Tail-call (sibling-call) optimization absent in ee-gcc 2.96.
# See tough_nuts/func_00106A98/notes.md.
func_00106A98  # no-TCO: ee-gcc emits prologue/epilogue + j, original tail-jumps
func_00106AA8  # no-TCO sibling of 00106A98

# ---- "set sdata to 1, return 1" — ee-gcc 2.96 splits the constant
# across two registers ($v0 return, $v1 store) instead of reusing $v0.
# See tough_nuts/func_0013B858/notes.md.
func_0013B858  # ret-1 + sdata-set (D_00633C90); regalloc differs
func_0013F878  # ret-1 + struct-field-set (a0+0x18); regalloc differs
func_0018CED0  # ret-1 + sdata-byte-set (D_00633D9C)
func_0018CEE0  # ret-1 + sdata-byte-set (D_00633D8C)
func_0018CFD0  # ret-1 + sdata-byte-set (D_00633D8D)
func_0018CFE0  # ret-1 + sdata-set (D_00632704)
func_00192040  # ret-1 + sdata-set (D_00632780)
func_0019E1C0  # ret-1 + struct-field-set (a0+0xB8)
func_001B0A58  # ret-1 + sdata-set (D_00633024)
func_001FAA30  # ret-1 + sdata-set (D_006337B8)

# ---- daddu vs or for register-zero / register-move.
# ee-gcc 2.96 emits `or $vN,$zero,$zero` to materialize 0; original ICO
# uses `daddu $vN,$zero,$zero`. Same semantics, different encoding.
# Affects "store side effects, return 0" leaves and "pass 0 as Nth arg"
# wrappers. See decomp/NOTES.md "Patterns parked".
func_00191D08  # daddu/or — return 0 with sdata side effects

# ---- la-macro-64-bit, separate lui+addiu vs lui+lw %lo split variant.
func_00244738  # la-macro-64-bit (lui+addiu vs lui+lw %lo split)
