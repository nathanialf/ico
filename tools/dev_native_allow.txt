# check_dev_native.py allowlist: one `path:function | reason` per line.
# A line here says the ROM or the listing proves the construct is what the
# developers wrote, and names the evidence. Nothing goes here for convenience.
ico2/seki/src/Texture.c:tex_initTextureSub | K&R definition forced by the ROM: tex_InitTexture calls it with no arguments (jal with a nop slot, listing Texture.c:1544) while the callee reads $a0 and $a1, and the definition precedes that caller (0x12E8F8 before 0x132CE0), so only an old-style definition compiles both; the listing's 12-line gap between the previous function's last statement (1430) and this one's first (1442) holds the two declaration lines an ANSI header would not.
sce/libgraph/graph003.c:asm:sceGsSetDefDispEnv | sce archive asm exception (user 2026-09-27): OPEN after five passes at strict 6 (rows c3p123 to c3p129, supervisor-validation-2026-09-27-sceGsSetDefDispEnv); the ROM's trap-before-dsll order in both f0 == 1 arms (words 53-55, 95-97) needs one zero-byte instruction reading the sum between the addiu and the andi in both schedulers, a same-register sign extension combine folds in every C spelling measured, the public DISPLAY bitfield record included (open1 B1 strict 151); the empty asm with the sum as its operand is the only construct measured to reproduce the bytes (strict 0, whole object identical to HEAD's).
sce/libsndn2/sound.c:asm:SgStAdpcmOpen | sce archive asm exception (user 2026-09-27): OPEN by the user's one-off skip after twelve passes and a fable round at strict 14 to 21 (rows c3p7 to c3p121, supervisor-audit-2026-09-27-SgStAdpcmOpen); the ROM's `lui; lui` ahead of `lbu $5,0($16)` needs one zero-byte instruction writing the record between the 0xC read and the other five reads in both schedulers, and the port's zero-byte emitters were enumerated with none reachable from C; the empty asm with the record as its memory output is the only construct measured to reproduce the bytes (strict 0, whole object identical to HEAD's).
