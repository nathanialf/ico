# Python tooling for ico (PS2) decomp. Pinning is light — re-pin if a
# CI run breaks.
splat64
spimdisasm
rabbitizer>=1.12,<2
mapfile-parser>=2.7,<3
pyelftools
pyyaml
capstone
intervaltree
colorama
tqdm
pycdlib
# n64img / pygfxd / crunch64 are not used for PS2 matching but splat eagerly
# imports its n64 segtypes at module-load time regardless of platform: ps2,
# so without them `splat --help` (and therefore `make split`) fails with
# ModuleNotFoundError. Track upstream — if splat lazy-loads platform
# segtypes someday, these can drop.
n64img
pygfxd
crunch64
# Note: asm-differ and decomp-permuter are git projects, not PyPI packages,
# fetched as submodules under lib/ by tools/setup.sh. m2c is also a
# submodule, installed editable into the venv.
