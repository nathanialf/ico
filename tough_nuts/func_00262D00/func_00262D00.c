/* func_00262D00 — NO recovered match. Still INCLUDE_ASM in common/src/PObj.c.
 * The cancelled bulk-campaign snapshot captured only callers/context, not a
 * decompiled body. Heavily used (varargs command dispatcher). See notes.md. */
