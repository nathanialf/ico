#include "regpin.h"

extern int D_004C7710[];

int func_001FBC18(void)
{
    register int *base REG("$3") = D_004C7710;
    int idx = base[0];
    int *p = base + idx;
    int end_off = p[1];
    char *cur = (char *)base[4];
    cur -= 0x80000;
    return (end_off - (int)cur) >> 4;
}
