extern char D_006A98B0[];

void func_00144208(int a0, float f12)
{
    char *entry;
    short id;
    int shifted;
    entry = &D_006A98B0[(a0 & 0xFF) * 64];
    id = *(short *)(entry + 0x10);
    if (id >= 0) {
        shifted = a0 >> 8;
        if (shifted == *(unsigned short *)entry) {
            *(float *)(entry + 0x18) = f12;
        }
    }
}
