# Functions whose `.p2align 3` (8-byte) labels must be demoted to
# `.p2align 2` (4-byte) — an if-merge label lands at VMA mod 8 == 4 and
# the 8-byte alignment would pad it with a stray nop the original lacks.
# Used instead of a per-file `-malign-loops=2` when the same TU also
# holds a function with a real loop top that needs the 8-byte pad.
# See tools/postprocess_demote_p2align.py.
#
# One function symbol per line.

# ios/cdvd.c: func_00131560's if-merge ($L7) is at mod-8==4; demote it so
# cdvd.c can build under default flags (which func_001320E8's read-chunk
# loop needs for its 8-byte loop-top pad).
func_00131560
