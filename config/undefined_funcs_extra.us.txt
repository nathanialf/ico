/* Hand-curated symbol overrides used by the linker.
 * Splat-generated `undefined_syms_auto.us.txt` is regenerated on every
 * `make setup`; entries here persist across that step.
 */

/* __main references 0x71EB68 via lui+lw with offset -0x1498 of
 * 0x720000.  Splat names the nearby symbol `D_71EB70` (without leading
 * zero) and does not register a separate symbol for the byte before it.
 */
D_0071EB68 = 0x71EB68;
