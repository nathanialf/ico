# Wrap c.{eq,lt,le}.{s,d} + bc1[ft]l? in .set noreorder to suppress
# ee-as auto-insertion of FCC hazard nop between them.
#
# Use when orig has NO nop between FCC compare and branch (i.e. orig
# was assembled by an ee-as build that did not enforce the hazard at
# this site), but our ee-as 2.10 auto-fills.
#
# Listed FILE_OFF (or coalesced-TU @func_ entries) get
# tools/postprocess_fcc_noreorder.py applied to the gcc-emitted .s.
enemy @func_001CE5F8
