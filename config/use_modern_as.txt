// TUs assembled with MODERN gas instead of the default period assembler
// (ee-as 2.9-991111). compile_c.sh flattens their .include'd splat siblings
// and translates the bare ACC/Q/R VU0 spelling to the $-form modern gas
// requires (preprocess_old_as.py --modern), so the whole input — including
// the included files — is in the modern dialect.
//
// 2026-08-01: enemy, Packet, vendor_2418A0 came OFF this list. Their matched
// C left a bare `j <sym>` / `beq` / `jr ra` with an UNFILLED delay slot in
// gcc's reorder-mode output; ee-as 2.9-991111 performs no delay-slot filling
// at all (probe: lw/addu before j/jal/jr, local or external — always nop),
// so only modern gas's own filling made those functions "match". Those 8
// functions (1 in enemy, 2 in Packet, 5 in vendor_2418A0) were reverted to
// INCLUDE_ASM — they were matched by ASSEMBLER, not by SOURCE. The recovered
// C is stashed for re-matching under
// /primary/home/n/.claude/projects/-primary-dev-ico/delayslot_queue/ .
//
// 2026-08-01: vendor_252D28 came OFF this list (it was the last entry).
// Its three period-assembler failures, for the record:
//   (1) splat's `enddlabel` left a `glabel`'s `.ent` unclosed, so the next
//       `.ent` died with "second .ent directive found before .end" — fixed
//       in include/labels.inc (0ac3cb51).
//   (2) ee-as 2.9-991111's R5900 SHORT-LOOP-ERRATUM pass pads a backward
//       branch with nops (up to 5, targeting a >=6-insn loop body) even
//       inside `.set noreorder` (probe: a 2-insn noreorder loop gets 3 nops
//       inserted before the branch). Its branch-to-label distance
//       computation is position-dependent and broke for func_00254328 once
//       enough file content preceded the block (standalone: clean; padding
//       8 nops at file start: clean; the believed distance shrank by 1 per
//       instruction added before the label) — it padded 2x5 nops before the
//       two backward branches of a ~32-insn loop. FIX (in the C's __asm__
//       block): reference the loop label through a forward-defined alias
//       (`.L0025071C_f = .L0025071C` after the branches) — an undefined-at-
//       parse-time target makes the pass skip the check, and the alias
//       resolves to identical bytes at write-out. No assembler flag exists
//       to disable the pass (it is tied to -mcpu=5900).
//   (3) func_00254CF8's `volatile` store blocked ee-gcc's own delay-slot
//       fill, so its match relied on MODERN gas filling the `jr $31` slot
//       (ee-as 2.9-991111 never fills). Dropping the volatile makes ee-gcc
//       emit `j $31; sw` itself, matching the ROM — same "matched by
//       assembler, not by source" class as the 8 reverted funcs above.
// Failure (2) is position-dependent: if a future edit to this TU shifts
// content so the pass mis-fires on ANOTHER backward branch (including
// splat-owned INCLUDE_ASM loops), the SHA gate will fail loudly; the fix
// there is the same forward-alias spelling (only possible in hand-asm) or
// re-listing the TU here.
