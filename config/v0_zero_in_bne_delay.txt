# Files where `postprocess_v0_zero_in_bne_delay.py` should run.
# Each line is the .c stem (case-sensitive) of a translation unit.
#
# Pattern: ee-gcc with -fno-schedule-insns emits the return-value
# `daddu $2,$0,$0` between `ld $31` and `ld $sN` in the epilogue, but
# the original codegen schedules it into the delay slot of the early-
# exit `bne $2,$0,$L`. See the postprocess's docstring for the full
# pattern + transformation.
#
# Add an entry only when adding the -fno-schedule-insns extra_cflag
# isn't enough on its own to produce the matching epilogue shape.

# Retirement-trial 2026-05-21: 30+ attempts. Root cause is gas's
# `.set reorder` refusing to fill bne delay with daddu $v0,$0,$0 because
# the delay-slot insn writes bne's compare register (MIPS semantics
# permit it but gas is conservative). Postprocess wraps with
# `.set noreorder/nomacro` to force gas.
# Tried: 9 source rewrites, 18 compiler flag combos, inline-asm-wrapped
# branch. gcc owns branch generation; user asm can't inject .set noreorder
# at gcc's branch emission point.
165B28
166870
