# Files (TU basename or file_off) whose `beq`/`bne` delay slots should
# be filled with the immediately-following instruction. ee-as 2.10 is
# conservative on plain beq/bne under `.set reorder` and inserts a nop
# rather than promoting the next insn. Most original ICO codegen
# actually wanted the nop (see §8.5's deliberate beq/bne exclusion),
# but a handful of functions have a real `beq + sw` fill. List those
# here.
#
# Format: <basename> on its own line. `#` comments OK.

kanban @func_001B04E0
