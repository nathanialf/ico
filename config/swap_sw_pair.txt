# Per-file post-process: swap two `sw` stores around the `j $31` epilogue.
# ee-gcc 2.9-991111 reorders independent stores by access pattern (writes
# the recently-read address first); some original ICO functions kept
# source-order. Same memory effect, different byte encoding.
#
# Files listed here get the swap applied via tools/postprocess_sw_pair.py.
# Format: <FILE_OFF_HEX> [# optional comment]

14BB90  # func_0024BB90: getset pair on D_00717758[5]/[6]
14BBA8  # func_0024BBA8: getset pair on D_00717758[3]/[4]
